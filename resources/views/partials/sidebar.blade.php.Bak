<!-- resources/views/layouts/sidebar.blade.php -->
<div class="h-screen w-64 bg-gradient-to-b from-slate-900 via-indigo-900 to-slate-800 text-white shadow-lg flex flex-col">
    <!-- Brand -->
    <div class="px-6 py-5 border-b border-indigo-700">
        <h2 class="text-2xl font-bold tracking-wide text-indigo-300">Infanect</h2>
        <p class="text-xs text-slate-400">Futuristic Dashboard</p>
    </div>

    <!-- Sidebar Navigation -->
    <nav class="flex-1 px-2 py-4 overflow-y-auto space-y-2" x-data="{ openMenu: null }">

        <!-- Dashboard Overview -->
        <a href="{{ route('dashboard') }}"
           class="flex items-center px-4 py-3 rounded-lg transition-all duration-200
           {{ request()->routeIs('dashboard') ? 'bg-indigo-600 text-white' : 'text-slate-300 hover:bg-indigo-700 hover:text-white' }}">
            <span class="mr-3"></span> Dashboard Overview
        </a>

        @if(auth()->user()->isSuperAdmin())
            <!-- ADMIN MENU -->
            <x-sidebar.admin :openMenu="openMenu" />

        @elseif(auth()->user()->isManager())
            <!-- MANAGER MENU -->
            <div>
                <a href="{{ route('manager.team') }}" class="block px-4 py-3 rounded-lg text-slate-300 hover:bg-indigo-700 hover:text-white">
                    Team Management
                </a>
                <a href="{{ route('manager.reports') }}" class="block px-4 py-3 rounded-lg text-slate-300 hover:bg-indigo-700 hover:text-white">
                    Reports & Insights
                </a>
            </div>

        @elseif(auth()->user()->isServiceProvider())
            <!-- PROVIDER MENU -->
            <div>
                <a href="{{ route('provider.dashboard') }}" class="block px-4 py-3 rounded-lg text-slate-300 hover:bg-indigo-700 hover:text-white">
                    Dashboard
                </a>
                <a href="{{ route('provider.services.index') }}" class="block px-4 py-3 rounded-lg text-slate-300 hover:bg-indigo-700 hover:text-white">
                    My Services
                </a>
                <a href="{{ route('provider.bookings.index') }}" class="block px-4 py-3 rounded-lg text-slate-300 hover:bg-indigo-700 hover:text-white">
                    Bookings
                </a>
                <a href="{{ route('provider.payments') }}" class="block px-4 py-3 rounded-lg text-slate-300 hover:bg-indigo-700 hover:text-white">
                    Payments
                </a>
                <a href="{{ route('provider.support') }}" class="block px-4 py-3 rounded-lg text-slate-300 hover:bg-indigo-700 hover:text-white">
                    Support
                </a>
                <a href="{{ route('profile.edit') }}" class="block px-4 py-3 rounded-lg text-slate-300 hover:bg-indigo-700 hover:text-white">
                    Profile
                </a>
            </div>

        @elseif(auth()->user()->isActivityProvider())
            <!-- ACTIVITY PROVIDER MENU -->
            <div>
                <a href="{{ route('activities.index') }}" class="block px-4 py-3 rounded-lg text-slate-300 hover:bg-indigo-700 hover:text-white">
                    My Activities
                </a>
                <a href="{{ route('bookings.index') }}" class="block px-4 py-3 rounded-lg text-slate-300 hover:bg-indigo-700 hover:text-white">
                    Registrations
                </a>
                <a href="{{ route('analytics.activity.performance') }}" class="block px-4 py-3 rounded-lg text-slate-300 hover:bg-indigo-700 hover:text-white">
                    Activity Insights
                </a>
                <a href="{{ route('profile.edit') }}" class="block px-4 py-3 rounded-lg text-slate-300 hover:bg-indigo-700 hover:text-white">
                    Profile
                </a>
            </div>

        @elseif(auth()->user()->isEmployee())
            <!-- EMPLOYEE MENU -->
            <div>
                <a href="{{ route('employee.tasks') }}" class="block px-4 py-3 rounded-lg text-slate-300 hover:bg-indigo-700 hover:text-white">
                    My Tasks
                </a>
                <a href="{{ route('employee.schedule') }}" class="block px-4 py-3 rounded-lg text-slate-300 hover:bg-indigo-700 hover:text-white">
                    Schedule
                </a>
                <a href="{{ route('analytics.employee.performance') }}" class="block px-4 py-3 rounded-lg text-slate-300 hover:bg-indigo-700 hover:text-white">
                    Employee Insights
                </a>
                <a href="{{ route('profile.edit') }}" class="block px-4 py-3 rounded-lg text-slate-300 hover:bg-indigo-700 hover:text-white">
                    Profile
                </a>
            </div>

        @elseif(auth()->user()->isUser())
            <!-- CLIENT / END-USER MENU -->
            <div>
                <a href="{{ route('services.index') }}" class="block px-4 py-3 rounded-lg text-slate-300 hover:bg-indigo-700 hover:text-white">
                    Browse Services
                </a>
                <a href="{{ route('activities.index') }}" class="block px-4 py-3 rounded-lg text-slate-300 hover:bg-indigo-700 hover:text-white">
                    Browse Activities
                </a>
                <a href="{{ route('bookings.index') }}" class="block px-4 py-3 rounded-lg text-slate-300 hover:bg-indigo-700 hover:text-white">
                    My Bookings
                </a>
                <a href="{{ route('parenting-modules.index') }}" class="block px-4 py-3 rounded-lg text-slate-300 hover:bg-indigo-700 hover:text-white">
                    Parenting Modules
                </a>
                <a href="{{ route('training-modules.index') }}" class="block px-4 py-3 rounded-lg text-slate-300 hover:bg-indigo-700 hover:text-white">
                    Training Modules
                </a>
                <a href="{{ route('ai-chat.index') }}" class="block px-4 py-3 rounded-lg text-slate-300 hover:bg-indigo-700 hover:text-white">
                    AI Chat Support
                </a>
                <a href="{{ route('analytics.user.engagement') }}" class="block px-4 py-3 rounded-lg text-slate-300 hover:bg-indigo-700 hover:text-white">
                    My Insights & Analytics
                </a>
                <a href="{{ route('profile.edit') }}" class="block px-4 py-3 rounded-lg text-slate-300 hover:bg-indigo-700 hover:text-white">
                    Profile
                </a>
            </div>
        @endif
    </nav>
</div>
